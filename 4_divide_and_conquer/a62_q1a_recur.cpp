#include <iostream>
#include <vector>

using namespace std;

void recur(vector<vector<int>> &v, int a, int b, int top, in)

/*
void cout_recur(int a, int b) {
    if (a == 0) {
        cout << b << " ";
        return;
    }

    recur(a-1, b);
    recur(a-1, b-1);
    cout << "\n";
    recur(a-1, b+1);
    recur(a-1, b);
}
*/

int a, b;
int main() {
    cin >> a >> b;
    recur(a, b);

}