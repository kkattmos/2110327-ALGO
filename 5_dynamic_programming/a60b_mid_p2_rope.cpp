//
// Rope

#include <bits/stdc++.h>
using namespace std;

int ropeLength;
vector<int> targetLength(3);
int result = 0;

vector<int> memo;

int main() {
    cin >> ropeLength;
    memo.resize(ropeLength);
    for (int i=0; i<3; i++) cin >> targetLength[i];


}